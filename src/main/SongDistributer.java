public class SongDistributer {
    SpotifyAuth spotifyAuth = SpotifyAuth.getInstance();

    public Song distributer(String name) {
        Playlist pl = newPlaylist(); // pl temp playlist
        Song s = distributer.identify(name); // s = identify(name) from song identifier
        pl.remove(s); // remove the song from temp playlist
        append.s(Playlist) // add song to playlist parent
            return Playlist; // return playlist
}
